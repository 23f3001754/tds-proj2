# tds-proj2
LLM based project for TDS (P2)

# Quiz Solver (Flask + Playwright)

Set `QUIZ_SECRET` in the environment (do NOT commit it). Deploy to Render/Railway and use the public HTTPS URL in the form.

See README in repo root for full commands.
